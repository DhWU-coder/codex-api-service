# OAuth 登录会话生命周期修复实施计划

> 按已确认设计直接实施。遵循测试驱动：先补会失败的行为测试，再完成实现。按项目约定，本计划不执行 git add、commit 或 push。

## 任务一：后端登录管理器生命周期

**文件：**

- 修改：`tests/test_oauth_login.py`
- 修改：`codex_api_service/oauth_login.py`

**步骤：**

1. 添加测试，验证重复启动返回当前活动会话而不是抛出冲突。
2. 添加测试，验证可读取活动会话且完成后返回空。
3. 添加测试，验证初始化只清理 `pending` 遗留内容。
4. 添加测试，验证取消会等待任务结束并终止子进程。
5. 添加测试，验证登录超时后进程被终止、会话失败且临时目录删除。
6. 运行测试确认新增行为尚未实现。
7. 实现幂等启动、活动会话读取、超时、可靠取消、关闭和遗留清理。
8. 重跑测试直至通过。

## 任务二：路由与账号池关闭联动

**文件：**

- 修改：`tests/test_app.py`
- 修改：`tests/test_oauth_pool.py`
- 修改：`codex_api_service/app.py`
- 修改：`codex_api_service/oauth_pool.py`

**步骤：**

1. 添加活动会话接口和重复启动路由测试。
2. 扩展账号池关闭测试，要求同时关闭登录管理器。
3. 运行相关测试确认失败。
4. 注册固定活动会话路由，并在账号池关闭阶段关闭登录管理器。
5. 重跑相关测试直至通过。

## 任务三：前端 API 与交互

**文件：**

- 修改：`frontend/src/api.test.ts`
- 修改：`frontend/src/App.test.tsx`
- 修改：`frontend/src/api.ts`
- 修改：`frontend/src/App.tsx`
- 修改：`frontend/src/styles.css`

**步骤：**

1. 添加 API 测试，验证活动会话读取和取消请求的路径、方法与鉴权。
2. 添加界面测试，验证初始化恢复活动登录和取消操作。
3. 添加界面测试，验证重复发起登录能接管后端返回的同一会话。
4. 运行新增测试确认失败。
5. 实现 API 辅助函数、单会话轮询器、初始化恢复和取消按钮。
6. 重跑前端测试直至通过。

## 任务四：完整回归验证

**步骤：**

1. 运行 OAuth 登录及账号池相关后端测试。
2. 运行后端全量测试。
3. 运行前端全量测试。
4. 构建前端生产资源。
5. 检查工作区差异，确认未触碰永久 OAuth 凭据，且未执行提交或推送。
