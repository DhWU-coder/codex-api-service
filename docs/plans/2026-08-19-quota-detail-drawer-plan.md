# 额度详情抽屉实施计划

> 按已确认设计直接实施。先添加行为测试，再完成最小实现。本次不执行 git add、commit 或 push。

## 任务一：锁定主页面与抽屉行为

**文件：**

- 修改：`frontend/src/App.test.tsx`

**步骤：**

1. 修改额度状态页测试，要求额外模型额度初始不可见。
2. 点击指定账号的 Codex 主额度卡片，断言右侧额度详情抽屉出现。
3. 断言抽屉同时展示主额度和 GPT-5.3-Codex-Spark 等额外额度。
4. 添加关闭按钮、遮罩和 `Escape` 关闭测试。
5. 添加单账号兼容模式测试。
6. 运行新增测试，确认当前实现不满足要求。

## 任务二：实现主额度入口与额度详情抽屉

**文件：**

- 修改：`frontend/src/App.tsx`
- 修改：`frontend/src/styles.css`

**步骤：**

1. 为主额度卡片增加可选按钮语义、键盘操作和详情提示。
2. 增加当前额度详情选择状态，并从最新额度快照派生抽屉数据。
3. 移除主页面额外额度卡片循环。
4. 增加右侧额度详情抽屉，依次渲染主额度和全部额外额度。
5. 增加关闭按钮、遮罩和 `Escape` 关闭逻辑。
6. 补充交互卡片与抽屉内容样式，额度详情在桌面端使用 60% 视口宽度。
7. 在 820px 及以下恢复全宽，并确保请求详情抽屉宽度不受影响。

## 任务三：回归验证与服务更新

**步骤：**

1. 运行 `App.test.tsx` 定向测试。
2. 运行前端全量测试。
3. 运行前端生产构建。
4. 检查差异，确认没有后端刷新逻辑变更。
5. 重启本地服务并验证健康状态与最新静态资源。
